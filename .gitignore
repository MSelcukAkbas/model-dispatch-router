# --- SECRETS ---------------------------------------------------------------
skills/model-dispatch-router/scripts/accounts.sh

# --- Runtime state ----------------------------------------------------------
.agentmind/
.agent-logs/
.worktrees/
.model-dispatch-router.json
graphify-out/
.data/
.remember/
snapshots/
*.db
*.db-wal
*.db-shm

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# --- Generated / disposable -------------------------------------------------
# (plugins/ directory is tracked in Git for ready-to-use distributions)

# --- Editor & machine-local state ------------------------------------------
.vscode/
CLAUDE.md
# Hook installs are machine-local: `am hooks-install` writes these.
.claude/settings.json
.codex/hooks.json
.agents/
old_model-dispatch/
.gitignore
