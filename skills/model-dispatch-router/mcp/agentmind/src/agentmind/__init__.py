"""AgentMind — orchestration kernel.

Phase 0 scope: one event log (L0) plus the tables Phase 1's overlay graph
will join against. See the plan file and the ideal-system definition for the
layer stack this belongs to.
"""

__version__ = "0.1.0"
